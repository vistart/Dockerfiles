name: Build and Deploy to Aliyun Container Registry

on:
  push:
    branches: [ php-official ]

jobs:
  build_8_0_cli:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v1
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      -
        name: Login to Aliyun Container Registry Hongkong
        uses: docker/login-action@v1 
        with:
          registry: registry.cn-hongkong.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Shanghai
        uses: docker/login-action@v1 
        with:
          registry: registry.cn-shanghai.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Build and push
        uses: docker/build-push-action@v2
        with:
          context: php
          platforms: linux/386,linux/amd64,linux/arm/v6,linux/arm/v7,linux/arm64,linux/ppc64le,linux/s390x
          push: true
          build-args: 8.0-cli
          tags: |
            registry.cn-hongkong.aliyuncs.com/vistart_public/php_official:cli
            registry.cn-hongkong.aliyuncs.com/vistart_public/php_official:8.0-cli
            registry.cn-shanghai.aliyuncs.com/vistart_public/php_official:cli
            registry.cn-shanghai.aliyuncs.com/vistart_public/php_official:8.0-cli
      -
        name: Image digest
        run: echo ${{ steps.docker_build.outputs.digest }}
  build_7_4_cli:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v1
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      -
        name: Login to Aliyun Container Registry Hongkong
        uses: docker/login-action@v1 
        with:
          registry: registry.cn-hongkong.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Shanghai
        uses: docker/login-action@v1 
        with:
          registry: registry.cn-shanghai.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Build and push
        uses: docker/build-push-action@v2
        with:
          context: php
          platforms: linux/386,linux/amd64,linux/arm/v6,linux/arm/v7,linux/arm64,linux/ppc64le,linux/s390x
          push: true
          build-args: 7.4-cli
          tags: |
            registry.cn-hongkong.aliyuncs.com/vistart_public/php_official:7.4-cli
            registry.cn-shanghai.aliyuncs.com/vistart_public/php_official:7.4-cli
      -
        name: Image digest
        run: echo ${{ steps.docker_build.outputs.digest }}
  build_7_3_cli:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v1
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      -
        name: Login to Aliyun Container Registry Hongkong
        uses: docker/login-action@v1 
        with:
          registry: registry.cn-hongkong.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Shanghai
        uses: docker/login-action@v1 
        with:
          registry: registry.cn-shanghai.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Build and push
        uses: docker/build-push-action@v2
        with:
          context: php
          platforms: linux/386,linux/amd64,linux/arm/v6,linux/arm/v7,linux/arm64,linux/ppc64le,linux/s390x
          push: true
          build-args: 7.3-cli
          tags: |
            registry.cn-hongkong.aliyuncs.com/vistart_public/php_official:7.3-cli
            registry.cn-shanghai.aliyuncs.com/vistart_public/php_official:7.3-cli
      -
        name: Image digest
        run: echo ${{ steps.docker_build.outputs.digest }}
  build_8_0_apache:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v1
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      -
        name: Login to Aliyun Container Registry Hongkong
        uses: docker/login-action@v1 
        with:
          registry: registry.cn-hongkong.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Shanghai
        uses: docker/login-action@v1 
        with:
          registry: registry.cn-shanghai.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Build and push
        uses: docker/build-push-action@v2
        with:
          context: php
          platforms: linux/386,linux/amd64,linux/arm/v6,linux/arm/v7,linux/arm64,linux/ppc64le,linux/s390x
          push: true
          build-args: 8.0-cli
          tags: |
            registry.cn-hongkong.aliyuncs.com/vistart_public/php_official:apache
            registry.cn-hongkong.aliyuncs.com/vistart_public/php_official:8.0-apache
            registry.cn-shanghai.aliyuncs.com/vistart_public/php_official:apache
            registry.cn-shanghai.aliyuncs.com/vistart_public/php_official:8.0-apache
      -
        name: Image digest
        run: echo ${{ steps.docker_build.outputs.digest }}
  build_7_4_apache:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v1
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      -
        name: Login to Aliyun Container Registry Hongkong
        uses: docker/login-action@v1 
        with:
          registry: registry.cn-hongkong.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Shanghai
        uses: docker/login-action@v1 
        with:
          registry: registry.cn-shanghai.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Build and push
        uses: docker/build-push-action@v2
        with:
          context: php
          platforms: linux/386,linux/amd64,linux/arm/v6,linux/arm/v7,linux/arm64,linux/ppc64le,linux/s390x
          push: true
          build-args: 7.4-cli
          tags: |
            registry.cn-hongkong.aliyuncs.com/vistart_public/php_official:7.4-apache
            registry.cn-shanghai.aliyuncs.com/vistart_public/php_official:7.4-apache
      -
        name: Image digest
        run: echo ${{ steps.docker_build.outputs.digest }}
  build_7_3_apache:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v1
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      -
        name: Login to Aliyun Container Registry Hongkong
        uses: docker/login-action@v1 
        with:
          registry: registry.cn-hongkong.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Shanghai
        uses: docker/login-action@v1 
        with:
          registry: registry.cn-shanghai.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Build and push
        uses: docker/build-push-action@v2
        with:
          context: php
          platforms: linux/386,linux/amd64,linux/arm/v6,linux/arm/v7,linux/arm64,linux/ppc64le,linux/s390x
          push: true
          build-args: 7.3-cli
          tags: |
            registry.cn-hongkong.aliyuncs.com/vistart_public/php_official:7.3-apache
            registry.cn-shanghai.aliyuncs.com/vistart_public/php_official:7.3-apache
      -
        name: Image digest
        run: echo ${{ steps.docker_build.outputs.digest }}
