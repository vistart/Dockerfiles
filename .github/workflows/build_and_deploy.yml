name: php - Build and Deploy to Aliyun Container Registry

on:
  push:
    branches: [ php ]
  schedule:
    - cron: '00 21 6 * *'

jobs:
  build_8_1_cli:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      -
        name: Login to Aliyun Container Registry Hongkong
        uses: docker/login-action@v3 
        with:
          registry: registry.cn-hongkong.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Shanghai
        uses: docker/login-action@v3 
        with:
          registry: registry.cn-shanghai.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Tokyo
        uses: docker/login-action@v3 
        with:
          registry: registry.ap-northeast-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry London
        uses: docker/login-action@v3 
        with:
          registry: registry.eu-west-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Silicon Valley
        uses: docker/login-action@v3 
        with:
          registry: registry.us-west-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Frankfurt
        uses: docker/login-action@v3 
        with:
          registry: registry.eu-central-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Sydney
        uses: docker/login-action@v3 
        with:
          registry: registry.ap-southeast-2.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Dubai
        uses: docker/login-action@v3 
        with:
          registry: registry.me-east-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Singapore
        uses: docker/login-action@v3 
        with:
          registry: registry.ap-southeast-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Build and push
        id: build_and_push_8_1_cli
        uses: docker/build-push-action@v5
        with:
          context: php/cli
          file: php/cli/Dockerfile
          platforms: linux/386,linux/amd64,linux/arm64,linux/ppc64le
          push: true
          build-args: |
            TAG=8.1-cli
          tags: |
            registry.cn-hongkong.aliyuncs.com/vistart_public/php:8.1-cli
            registry.cn-shanghai.aliyuncs.com/vistart_public/php:8.1-cli
            registry.ap-northeast-1.aliyuncs.com/vistart_public/php:8.1-cli
            registry.eu-west-1.aliyuncs.com/vistart_public/php:8.1-cli
            registry.us-west-1.aliyuncs.com/vistart_public/php:8.1-cli
            registry.eu-central-1.aliyuncs.com/vistart_public/php:8.1-cli
            registry.ap-southeast-2.aliyuncs.com/vistart_public/php:8.1-cli
            registry.me-east-1.aliyuncs.com/vistart_public/php:8.1-cli
            registry.ap-southeast-1.aliyuncs.com/vistart_public/php:8.1-cli
            registry.cn-hongkong.aliyuncs.com/vistart_public/php:cli
            registry.cn-shanghai.aliyuncs.com/vistart_public/php:cli
            registry.ap-northeast-1.aliyuncs.com/vistart_public/php:cli
            registry.eu-west-1.aliyuncs.com/vistart_public/php:cli
            registry.us-west-1.aliyuncs.com/vistart_public/php:cli
            registry.eu-central-1.aliyuncs.com/vistart_public/php:cli
            registry.ap-southeast-2.aliyuncs.com/vistart_public/php:cli
            registry.me-east-1.aliyuncs.com/vistart_public/php:cli
            registry.ap-southeast-1.aliyuncs.com/vistart_public/php:cli
      -
        name: Image digest
        run: echo ${{ steps.build_and_push_8_1_cli.outputs.digest }}
  build_8_0_cli:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      -
        name: Login to Aliyun Container Registry Hongkong
        uses: docker/login-action@v3 
        with:
          registry: registry.cn-hongkong.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Shanghai
        uses: docker/login-action@v3 
        with:
          registry: registry.cn-shanghai.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Tokyo
        uses: docker/login-action@v3 
        with:
          registry: registry.ap-northeast-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry London
        uses: docker/login-action@v3 
        with:
          registry: registry.eu-west-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Silicon Valley
        uses: docker/login-action@v3 
        with:
          registry: registry.us-west-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Frankfurt
        uses: docker/login-action@v3 
        with:
          registry: registry.eu-central-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Sydney
        uses: docker/login-action@v3 
        with:
          registry: registry.ap-southeast-2.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Dubai
        uses: docker/login-action@v3 
        with:
          registry: registry.me-east-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Singapore
        uses: docker/login-action@v3 
        with:
          registry: registry.ap-southeast-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Build and push
        id: build_and_push_8_0_cli
        uses: docker/build-push-action@v5
        with:
          context: php/cli
          platforms: linux/386,linux/amd64,linux/arm64,linux/ppc64le
          push: true
          build-args: |
            TAG=8.0-cli
          tags: |
            registry.cn-hongkong.aliyuncs.com/vistart_public/php:8.0-cli
            registry.cn-shanghai.aliyuncs.com/vistart_public/php:8.0-cli
            registry.ap-northeast-1.aliyuncs.com/vistart_public/php:8.0-cli
            registry.eu-west-1.aliyuncs.com/vistart_public/php:8.0-cli
            registry.us-west-1.aliyuncs.com/vistart_public/php:8.0-cli
            registry.eu-central-1.aliyuncs.com/vistart_public/php:8.0-cli
            registry.ap-southeast-2.aliyuncs.com/vistart_public/php:8.0-cli
            registry.me-east-1.aliyuncs.com/vistart_public/php:8.0-cli
            registry.ap-southeast-1.aliyuncs.com/vistart_public/php:8.0-cli
      -
        name: Image digest
        run: echo ${{ steps.build_and_push_8_0_cli.outputs.digest }}
  build_8_2_cli:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      -
        name: Login to Aliyun Container Registry Hongkong
        uses: docker/login-action@v3 
        with:
          registry: registry.cn-hongkong.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Shanghai
        uses: docker/login-action@v3 
        with:
          registry: registry.cn-shanghai.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Tokyo
        uses: docker/login-action@v3 
        with:
          registry: registry.ap-northeast-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry London
        uses: docker/login-action@v3 
        with:
          registry: registry.eu-west-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Silicon Valley
        uses: docker/login-action@v3 
        with:
          registry: registry.us-west-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Frankfurt
        uses: docker/login-action@v3 
        with:
          registry: registry.eu-central-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Sydney
        uses: docker/login-action@v3 
        with:
          registry: registry.ap-southeast-2.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Dubai
        uses: docker/login-action@v3 
        with:
          registry: registry.me-east-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Singapore
        uses: docker/login-action@v3 
        with:
          registry: registry.ap-southeast-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Build and push
        id: build_and_push_8_2_cli
        uses: docker/build-push-action@v5
        with:
          context: php/cli
          file: php/cli/Dockerfile-8.2
          platforms: linux/386,linux/amd64,linux/arm64,linux/ppc64le
          push: true
          build-args: |
            TAG=8.2-cli
          tags: |
            registry.cn-hongkong.aliyuncs.com/vistart_public/php:8.2-cli
            registry.cn-shanghai.aliyuncs.com/vistart_public/php:8.2-cli
            registry.ap-northeast-1.aliyuncs.com/vistart_public/php:8.2-cli
            registry.eu-west-1.aliyuncs.com/vistart_public/php:8.2-cli
            registry.us-west-1.aliyuncs.com/vistart_public/php:8.2-cli
            registry.eu-central-1.aliyuncs.com/vistart_public/php:8.2-cli
            registry.ap-southeast-2.aliyuncs.com/vistart_public/php:8.2-cli
            registry.me-east-1.aliyuncs.com/vistart_public/php:8.2-cli
            registry.ap-southeast-1.aliyuncs.com/vistart_public/php:8.2-cli
      -
        name: Image digest
        run: echo ${{ steps.build_and_push_8_2_cli.outputs.digest }}
  build_8_3_cli:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      -
        name: Login to Aliyun Container Registry Hongkong
        uses: docker/login-action@v3 
        with:
          registry: registry.cn-hongkong.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Shanghai
        uses: docker/login-action@v3 
        with:
          registry: registry.cn-shanghai.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Tokyo
        uses: docker/login-action@v3 
        with:
          registry: registry.ap-northeast-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry London
        uses: docker/login-action@v3 
        with:
          registry: registry.eu-west-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Silicon Valley
        uses: docker/login-action@v3 
        with:
          registry: registry.us-west-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Frankfurt
        uses: docker/login-action@v3 
        with:
          registry: registry.eu-central-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Sydney
        uses: docker/login-action@v3 
        with:
          registry: registry.ap-southeast-2.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Dubai
        uses: docker/login-action@v3 
        with:
          registry: registry.me-east-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Singapore
        uses: docker/login-action@v3 
        with:
          registry: registry.ap-southeast-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Build and push
        id: build_and_push_8_3_cli
        uses: docker/build-push-action@v5
        with:
          context: php/cli
          file: php/cli/Dockerfile-8.3
          platforms: linux/386,linux/amd64,linux/arm64,linux/ppc64le
          push: true
          build-args: |
            TAG=8.3-cli
          tags: |
            registry.cn-hongkong.aliyuncs.com/vistart_public/php:8.3-cli
            registry.cn-shanghai.aliyuncs.com/vistart_public/php:8.3-cli
            registry.ap-northeast-1.aliyuncs.com/vistart_public/php:8.3-cli
            registry.eu-west-1.aliyuncs.com/vistart_public/php:8.3-cli
            registry.us-west-1.aliyuncs.com/vistart_public/php:8.3-cli
            registry.eu-central-1.aliyuncs.com/vistart_public/php:8.3-cli
            registry.ap-southeast-2.aliyuncs.com/vistart_public/php:8.3-cli
            registry.me-east-1.aliyuncs.com/vistart_public/php:8.3-cli
            registry.ap-southeast-1.aliyuncs.com/vistart_public/php:8.3-cli
      -
        name: Image digest
        run: echo ${{ steps.build_and_push_8_3_cli.outputs.digest }}
  build_8_1_apache:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      -
        name: Login to Aliyun Container Registry Hongkong
        uses: docker/login-action@v3 
        with:
          registry: registry.cn-hongkong.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Shanghai
        uses: docker/login-action@v3 
        with:
          registry: registry.cn-shanghai.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Tokyo
        uses: docker/login-action@v3 
        with:
          registry: registry.ap-northeast-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry London
        uses: docker/login-action@v3 
        with:
          registry: registry.eu-west-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Silicon Valley
        uses: docker/login-action@v3 
        with:
          registry: registry.us-west-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Frankfurt
        uses: docker/login-action@v3 
        with:
          registry: registry.eu-central-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Sydney
        uses: docker/login-action@v3 
        with:
          registry: registry.ap-southeast-2.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Dubai
        uses: docker/login-action@v3 
        with:
          registry: registry.me-east-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Singapore
        uses: docker/login-action@v3 
        with:
          registry: registry.ap-southeast-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Build and push
        id: build_and_push_8_1_apache
        uses: docker/build-push-action@v5
        with:
          context: php/apache
          file: php/apache/Dockerfile
          platforms: linux/386,linux/amd64,linux/arm64,linux/ppc64le
          push: true
          build-args: |
            TAG=8.1-apache
          tags: |
            registry.cn-hongkong.aliyuncs.com/vistart_public/php:8.1-apache
            registry.cn-shanghai.aliyuncs.com/vistart_public/php:8.1-apache
            registry.ap-northeast-1.aliyuncs.com/vistart_public/php:8.1-apache
            registry.eu-west-1.aliyuncs.com/vistart_public/php:8.1-apache
            registry.us-west-1.aliyuncs.com/vistart_public/php:8.1-apache
            registry.eu-central-1.aliyuncs.com/vistart_public/php:8.1-apache
            registry.ap-southeast-2.aliyuncs.com/vistart_public/php:8.1-apache
            registry.me-east-1.aliyuncs.com/vistart_public/php:8.1-apache
            registry.ap-southeast-1.aliyuncs.com/vistart_public/php:8.1-apache
            registry.cn-hongkong.aliyuncs.com/vistart_public/php:apache
            registry.cn-shanghai.aliyuncs.com/vistart_public/php:apache
            registry.ap-northeast-1.aliyuncs.com/vistart_public/php:apache
            registry.eu-west-1.aliyuncs.com/vistart_public/php:apache
            registry.us-west-1.aliyuncs.com/vistart_public/php:apache
            registry.eu-central-1.aliyuncs.com/vistart_public/php:apache
            registry.ap-southeast-2.aliyuncs.com/vistart_public/php:apache
            registry.me-east-1.aliyuncs.com/vistart_public/php:apache
            registry.ap-southeast-1.aliyuncs.com/vistart_public/php:apache
      -
        name: Image digest
        run: echo ${{ steps.build_and_push_8_1_apache.outputs.digest }}
  build_8_0_apache:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      -
        name: Login to Aliyun Container Registry Hongkong
        uses: docker/login-action@v3 
        with:
          registry: registry.cn-hongkong.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Shanghai
        uses: docker/login-action@v3 
        with:
          registry: registry.cn-shanghai.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Tokyo
        uses: docker/login-action@v3 
        with:
          registry: registry.ap-northeast-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry London
        uses: docker/login-action@v3 
        with:
          registry: registry.eu-west-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Silicon Valley
        uses: docker/login-action@v3 
        with:
          registry: registry.us-west-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Frankfurt
        uses: docker/login-action@v3 
        with:
          registry: registry.eu-central-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Sydney
        uses: docker/login-action@v3 
        with:
          registry: registry.ap-southeast-2.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Dubai
        uses: docker/login-action@v3 
        with:
          registry: registry.me-east-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Singapore
        uses: docker/login-action@v3 
        with:
          registry: registry.ap-southeast-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Build and push
        id: build_and_push_8_0_apache
        uses: docker/build-push-action@v5
        with:
          context: php/apache
          platforms: linux/386,linux/amd64,linux/arm64,linux/ppc64le
          push: true
          build-args: |
            TAG=8.0-apache
          tags: |
            registry.cn-hongkong.aliyuncs.com/vistart_public/php:8.0-apache
            registry.cn-shanghai.aliyuncs.com/vistart_public/php:8.0-apache
            registry.ap-northeast-1.aliyuncs.com/vistart_public/php:8.0-apache
            registry.eu-west-1.aliyuncs.com/vistart_public/php:8.0-apache
            registry.us-west-1.aliyuncs.com/vistart_public/php:8.0-apache
            registry.eu-central-1.aliyuncs.com/vistart_public/php:8.0-apache
            registry.ap-southeast-2.aliyuncs.com/vistart_public/php:8.0-apache
            registry.me-east-1.aliyuncs.com/vistart_public/php:8.0-apache
            registry.ap-southeast-1.aliyuncs.com/vistart_public/php:8.0-apache
      -
        name: Image digest
        run: echo ${{ steps.build_and_push_8_0_apache.outputs.digest }}
  build_8_2_apache:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      -
        name: Login to Aliyun Container Registry Hongkong
        uses: docker/login-action@v3 
        with:
          registry: registry.cn-hongkong.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Shanghai
        uses: docker/login-action@v3 
        with:
          registry: registry.cn-shanghai.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Tokyo
        uses: docker/login-action@v3 
        with:
          registry: registry.ap-northeast-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry London
        uses: docker/login-action@v3 
        with:
          registry: registry.eu-west-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Silicon Valley
        uses: docker/login-action@v3 
        with:
          registry: registry.us-west-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Frankfurt
        uses: docker/login-action@v3 
        with:
          registry: registry.eu-central-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Sydney
        uses: docker/login-action@v3 
        with:
          registry: registry.ap-southeast-2.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Dubai
        uses: docker/login-action@v3 
        with:
          registry: registry.me-east-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Singapore
        uses: docker/login-action@v3 
        with:
          registry: registry.ap-southeast-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Build and push
        id: build_and_push_8_2_apache
        uses: docker/build-push-action@v5
        with:
          context: php/apache
          file: php/apache/Dockerfile-8.2
          platforms: linux/386,linux/amd64,linux/arm64,linux/ppc64le
          push: true
          build-args: |
            TAG=8.2-apache
          tags: |
            registry.cn-hongkong.aliyuncs.com/vistart_public/php:8.2-apache
            registry.cn-shanghai.aliyuncs.com/vistart_public/php:8.2-apache
            registry.ap-northeast-1.aliyuncs.com/vistart_public/php:8.2-apache
            registry.eu-west-1.aliyuncs.com/vistart_public/php:8.2-apache
            registry.us-west-1.aliyuncs.com/vistart_public/php:8.2-apache
            registry.eu-central-1.aliyuncs.com/vistart_public/php:8.2-apache
            registry.ap-southeast-2.aliyuncs.com/vistart_public/php:8.2-apache
            registry.me-east-1.aliyuncs.com/vistart_public/php:8.2-apache
            registry.ap-southeast-1.aliyuncs.com/vistart_public/php:8.2-apache
      -
        name: Image digest
        run: echo ${{ steps.build_and_push_8_2_apache.outputs.digest }}
  build_8_3_apache:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      -
        name: Login to Aliyun Container Registry Hongkong
        uses: docker/login-action@v3 
        with:
          registry: registry.cn-hongkong.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Shanghai
        uses: docker/login-action@v3 
        with:
          registry: registry.cn-shanghai.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Tokyo
        uses: docker/login-action@v3 
        with:
          registry: registry.ap-northeast-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry London
        uses: docker/login-action@v3 
        with:
          registry: registry.eu-west-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Silicon Valley
        uses: docker/login-action@v3 
        with:
          registry: registry.us-west-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Frankfurt
        uses: docker/login-action@v3 
        with:
          registry: registry.eu-central-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Sydney
        uses: docker/login-action@v3 
        with:
          registry: registry.ap-southeast-2.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Dubai
        uses: docker/login-action@v3 
        with:
          registry: registry.me-east-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Singapore
        uses: docker/login-action@v3 
        with:
          registry: registry.ap-southeast-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Build and push
        id: build_and_push_8_3_apache
        uses: docker/build-push-action@v5
        with:
          context: php/apache
          file: php/apache/Dockerfile-8.3
          platforms: linux/386,linux/amd64,linux/arm64,linux/ppc64le
          push: true
          build-args: |
            TAG=8.3-apache
          tags: |
            registry.cn-hongkong.aliyuncs.com/vistart_public/php:8.3-apache
            registry.cn-shanghai.aliyuncs.com/vistart_public/php:8.3-apache
            registry.ap-northeast-1.aliyuncs.com/vistart_public/php:8.3-apache
            registry.eu-west-1.aliyuncs.com/vistart_public/php:8.3-apache
            registry.us-west-1.aliyuncs.com/vistart_public/php:8.3-apache
            registry.eu-central-1.aliyuncs.com/vistart_public/php:8.3-apache
            registry.ap-southeast-2.aliyuncs.com/vistart_public/php:8.3-apache
            registry.me-east-1.aliyuncs.com/vistart_public/php:8.3-apache
            registry.ap-southeast-1.aliyuncs.com/vistart_public/php:8.3-apache
      -
        name: Image digest
        run: echo ${{ steps.build_and_push_8_3_apache.outputs.digest }}