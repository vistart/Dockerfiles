name: php - Build and Deploy PHP 8.0 CLI to Aliyun Container Registry

on:
  push:
    branches: [ php ]
  schedule:
    - cron: '05 20 * * Tue'

jobs:
  build_8_0_cli:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v1
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      -
        name: Login to Aliyun Container Registry Hongkong
        uses: docker/login-action@v1 
        with:
          registry: registry.cn-hongkong.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Shanghai
        uses: docker/login-action@v1 
        with:
          registry: registry.cn-shanghai.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
#      -
#        name: Login to Aliyun Container Registry Hohhot
#        uses: docker/login-action@v1 
#        with:
#          registry: registry.cn-huhehaote.aliyuncs.com
#          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
#          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Guangzhou
        uses: docker/login-action@v1 
        with:
          registry: registry.cn-guangzhou.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Chengdu
        uses: docker/login-action@v1 
        with:
          registry: registry.cn-chengdu.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Silicon Valley
        uses: docker/login-action@v1 
        with:
          registry: registry.us-west-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Frankfurt
        uses: docker/login-action@v1 
        with:
          registry: registry.eu-central-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Sydney
        uses: docker/login-action@v1 
        with:
          registry: registry.ap-southeast-2.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Dubai
        uses: docker/login-action@v1 
        with:
          registry: registry.me-east-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Login to Aliyun Container Registry Singapore
        uses: docker/login-action@v1 
        with:
          registry: registry.ap-southeast-1.aliyuncs.com
          username: ${{ secrets.ALIYUN_CR_VISTART_USERNAME }}
          password: ${{ secrets.ALIYUN_CR_VISTART_PASSWORD }}
      -
        name: Build and push
        id: build_and_push_8_0_cli
        uses: docker/build-push-action@v2
        with:
          context: php/cli
          platforms: linux/386,linux/amd64,linux/arm/v6,linux/arm/v7,linux/arm64,linux/ppc64le,linux/s390x
          push: true
          build-args: 8.0-cli
          tags: |
            registry.cn-hongkong.aliyuncs.com/vistart_public/php:8.0-cli
            registry.cn-shanghai.aliyuncs.com/vistart_public/php:8.0-cli
            registry.cn-guangzhou.aliyuncs.com/vistart_public/php:8.0-cli
            registry.cn-chengdu.aliyuncs.com/vistart_public/php:8.0-cli
            registry.us-west-1.aliyuncs.com/vistart_public/php:8.0-cli
            registry.eu-central-1.aliyuncs.com/vistart_public/php:8.0-cli
            registry.ap-southeast-2.aliyuncs.com/vistart_public/php:8.0-cli
            registry.me-east-1.aliyuncs.com/vistart_public/php:8.0-cli
            registry.ap-southeast-1.aliyuncs.com/vistart_public/php:8.0-cli
            registry.cn-hongkong.aliyuncs.com/vistart_public/php:cli
            registry.cn-shanghai.aliyuncs.com/vistart_public/php:cli
            registry.cn-guangzhou.aliyuncs.com/vistart_public/php:cli
            registry.cn-chengdu.aliyuncs.com/vistart_public/php:cli
            registry.us-west-1.aliyuncs.com/vistart_public/php:cli
            registry.eu-central-1.aliyuncs.com/vistart_public/php:cli
            registry.ap-southeast-2.aliyuncs.com/vistart_public/php:cli
            registry.me-east-1.aliyuncs.com/vistart_public/php:cli
            registry.ap-southeast-1.aliyuncs.com/vistart_public/php:cli
      -
        name: Image digest
        run: echo ${{ steps.build_and_push_8_0_cli.outputs.digest }}
