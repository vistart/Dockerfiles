# Dockerfiles
Dockerfiles

Considering that Docker Hub is not accessible in mainland China, this branch will transfer the latest tag of commonly used images to the Aliyun registry.

Currently, it will be executed once a day at 21:00.

Here are the regularly updated images and tags:

- Redis
- MySQL
- MariaDB
- PostgreSQL
- MongoDB
- Nginx